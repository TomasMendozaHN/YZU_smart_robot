# Important:

Inside this folder (once downloaded in your Jetbot) you must have:

1. A Pre-Trained Unity Model (in .pth format)

2. A Pre-Trained KeyPoints_RCNN model (in TensorRT Format)

3. The model with the KeyPoints information (in JSON format).
